server:
  port: 8901

dtm:
  invoke:
    mode: feign
spring:
  application:
    name: invoke
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true
